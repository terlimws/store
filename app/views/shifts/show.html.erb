<h1>Shift Details</h1>
<br>
  <table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>Employee</th>
        <th>Date</th>
        <th>Start Time</th>
        <th>End Time</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= @shift.employee.name %></td>
          <td><%= l @shift.date %></td>
          <td><%= l @shift.start_time, :format => :shifttime %></td>
          <td><%= @shift.end_time.nil? ? "Has not ended" : (l @shift.end_time, :format => :shifttime) %></td>
        </tr>
  
    </tbody>
  </table><p>
<p>
  <b>Notes</b><br>
  <%= "No notes found" if @shift.notes == '' %>
  <%= @shift.notes %>
</p>




<div class="form-actions">
  <% if cannot? :manage, Shift %><%= link_to_function 'Back', "history.back()", :class => 'btn'  %>
<% else %><%= link_to 'Back', shifts_path, :class => 'btn'  %><% end %>
  <% if can? :manage, Shift %>
  <%= link_to 'Edit', edit_shift_path(@shift), :class => 'btn' %>
  <%= link_to 'Delete', shift_path(@shift), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
<% end %>
</div>
